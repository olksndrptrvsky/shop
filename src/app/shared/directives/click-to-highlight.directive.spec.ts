import { ClickToHighlightDirective } from './click-to-highlight.directive';

describe('ClickToHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickToHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
